# Follow redirects
--location

# Attempt HTTP/3 Connection
--http3

# Don't overwrite existing files
--no-clobber

# Make curl parallel
--parallel

# Require TLS for FTP/IMAP/POP3/SMTP/LDAP
--ssl-reqd
